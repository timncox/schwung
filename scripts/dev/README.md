# Dev / one-off scripts

Tools used during past investigations; nothing in the build, install, or
release flow references them.

- `analyze_wavs.py` — PCM-dump cross-correlation from the latency-comp work
- `migrate-module-params.py` — one-time module param migration
- `test-safety-fixes.sh` — verification suite for the 2026-02 RT-safety round
- `test-auto-update.sh` — manual auto-update test helper
- `fix-ssh.sh` — SSH key cleanup helper
